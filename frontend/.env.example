# Copy this file to .env and fill in your values.
# Restart the dev server after changing .env.

# Verification email API – same as CareConnect-Android (api/sendVerificationEmail.js).
# Use the CareConnect Vercel project URL, e.g. https://care-connect-android.vercel.app/api/sendVerificationEmail
VITE_VERIFICATION_EMAIL_API_URL=

# Must match the CareConnect Vercel project env var ASSISTANCE_API_KEY.
# In Vercel (care-connect-android): Settings → Environment Variables → ASSISTANCE_API_KEY.
# Copy that value here so the API accepts our requests (X-API-Key header).
VITE_VERIFICATION_EMAIL_API_KEY=

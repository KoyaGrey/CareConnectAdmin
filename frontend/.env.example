# Copy this file to .env and fill in your values.
# Restart the dev server after changing .env.

# Verification email API (same endpoint as CareConnect app).
# Example: https://your-project.vercel.app/api/sendVerificationEmail
VITE_VERIFICATION_EMAIL_API_URL=

# Optional: only if your API requires an API key (sent as X-API-Key header)
VITE_VERIFICATION_EMAIL_API_KEY=
